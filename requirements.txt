pygame-ce>=2.5
customtkinter>=6.0.0
pillow>=10.0

# Only needed if you're using a real 4-button hardware controller
# (see firmware/endless_straight_controller.ino). Not required to
# just play with a keyboard.
pyserial>=3.5
